schema: openspec-change-state/v1
change: local-agent-session-status
status: applying
phase: apply
artifact_store:
  mode: hybrid
  history_authority: engram
engram:
  apply_progress_topic: sdd/local-agent-session-status/apply-progress
  tasks_topic: sdd/local-agent-session-status/tasks
task_progress:
  completed:
    - "1.1"
    - "1.2"
    - "1.3"
    - "2.1"
    - "2.2"
    - "2.3"
  remaining:
    - "3.1"
    - "3.2"
    - "3.3"
    - "3.4"
    - "4.1"
    - "4.2"
    - "4.3"
apply_progress:
  current_slice: Native ordinal 15 scanline-contract terminology cleanup for issue #32
  status: Replaced platform-sensitive PNG byte comparison with authoritative validated IHDR plus inflated filtered-scanline-byte comparison; gray-slot runtime behavior and hardware evidence are unchanged
  implementation: uncommitted
  verification: Explicit Node 24.18.0 initial RED then GREEN 13/13; ordinal-13 retry rejects trailing IDAT bytes and bounds scanlines; focused checks passed 19/19, typecheck and exact npm run verify passed 303/303, audit 0 vulnerabilities, package validation, and local runtime smoke; tracked assets/profile remained unchanged
  blockers: []
  requirement_amendment: unassigned slots gray; green only for assigned acknowledged/read sessions; exact N assigned colors plus 5-N gray; immediate gray release on pane disappearance; initial all-gray state
  next_action: Fresh Node 24 cross-platform CI/gate on PR #30, then merge under parent authorization; preserve 6/13 task checkbox state and do not mutate the native ledger
