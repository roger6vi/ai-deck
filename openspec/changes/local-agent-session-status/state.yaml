schema: openspec-change-state/v1
change: local-agent-session-status
status: applying
phase: apply
artifact_store:
  mode: hybrid
  history_authority: engram
engram:
  apply_progress_topic: sdd/local-agent-session-status/apply-progress
  tasks_topic: sdd/local-agent-session-status/tasks
task_progress:
  completed:
    - "1.1"
    - "1.2"
    - "1.3"
    - "2.1"
    - "2.2"
    - "2.3"
  remaining:
    - "3.1"
    - "3.2"
    - "3.3"
    - "3.4"
    - "4.1"
    - "4.2"
    - "4.3"
apply_progress:
  current_slice: Session state store (pure serialize/parse + atomic save)
  status: privacy-safe allowlist-validated serializer/parser plus a store that returns empty state on missing/corrupt/insecure files and writes 0o600 state.json via temp+rename under 0o700 runtime dir; reconciliation of stale panes deferred
  implementation: committed (249bd7a)
  verification: RED missing module; GREEN focused 17/17; full Node 24 npm run verify 339/339, strict typecheck, 0 vulnerabilities, package validation, bounded runtime smoke
  blockers: []
  requirement_amendment: unassigned slots gray; green only for assigned acknowledged/read sessions; exact N assigned colors plus 5-N gray; immediate gray release on pane disappearance; initial all-gray state
  next_action: Continue 3.1/3.2 remaining scope — per-adapter observers (Codex/OpenCode/Claude), pane reconciliation at startup, installers, and remaining safe commands; keep 3.1/3.2 unchecked until full scope done; do not mutate the native ledger
