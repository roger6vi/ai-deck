schema: openspec-change-state/v1
change: local-agent-session-status
status: applying
phase: apply
artifact_store:
  mode: hybrid
  history_authority: engram
engram:
  apply_progress_topic: sdd/local-agent-session-status/apply-progress
  tasks_topic: sdd/local-agent-session-status/tasks
task_progress:
  completed:
    - "1.1"
    - "1.2"
    - "1.3"
    - "2.1"
    - "2.2"
    - "2.3"
  remaining:
    - "3.1"
    - "3.2"
    - "3.3"
    - "3.4"
    - "4.1"
    - "4.2"
    - "4.3"
apply_progress:
  current_slice: Native ordinal 12 physical acceptance
  status: Physical acceptance passed; user confirmed three amber/two disabled gray and tracked pane disappearance released all five slots to gray
  implementation: uncommitted
  verification: Native ordinal 12 restart succeeded; three tracked sessions returned 204 for started and exact pane-disappeared; zero assignments, all-gray user confirmation, and fresh StreamDeck.log window had PID 149 alive with zero fixed errors
  blockers: []
  requirement_amendment: unassigned slots gray; green only for assigned acknowledged/read sessions; exact N assigned colors plus 5-N gray; immediate gray release on pane disappearance; initial all-gray state
  next_action: CI/merge PR #30 under existing authorization, then continue deferred 3.1/3.2 navigation, persistence, adapter, installer, recovery, and integration scope; preserve 6/13 task checkbox state and do not mutate the native ledger
