{
  "name": "ai-deck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "npm@11.16.0",
  "scripts": {
    "build": "node scripts/build-plugin.mjs",
    "build:rollup": "rollup -c",
    "assets:build": "node scripts/build-assets.mjs",
    "profile:build": "node scripts/build-profile.mjs",
    "generate": "npm run assets:build && npm run profile:build",
    "check:generated": "node scripts/check-generated.mjs",
    "validate:profile": "node scripts/validate-profile.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "validate:plugin": "streamdeck validate com.gentleman.ai-deck.sdPlugin --no-update-check",
    "check:package": "node scripts/check-package.mjs",
    "smoke:runtime": "node scripts/runtime-smoke.mjs",
    "restart:plugin": "streamdeck restart com.gentleman.ai-deck",
    "uninstall:plugin": "streamdeck unlink --delete com.gentleman.ai-deck",
    "pack": "npm run build && npm run generate && npm run check:generated && npm run validate:profile && npm run validate:plugin && rm -rf com.gentleman.ai-deck.sdPlugin/bin.previous com.gentleman.ai-deck.sdPlugin/bin.next .package-stage && mkdir .package-stage && cp -R com.gentleman.ai-deck.sdPlugin .package-stage/ && streamdeck pack .package-stage/com.gentleman.ai-deck.sdPlugin --output dist --force --no-update-check && rm -rf .package-stage && npm run check:package",
    "audit:production": "npm audit --omit=dev --audit-level=low",
    "verify": "npm test && npm run typecheck && npm run audit:production && npm run pack && npm run smoke:runtime"
  },
  "dependencies": {
    "@elgato/streamdeck": "2.1.0"
  },
  "devDependencies": {
    "@elgato/cli": "1.7.4",
    "@rollup/plugin-commonjs": "29.0.3",
    "@rollup/plugin-node-resolve": "16.0.3",
    "@rollup/plugin-typescript": "12.3.0",
    "@types/node": "24.13.3",
    "@zip.js/zip.js": "2.8.34",
    "rollup": "4.62.3",
    "tslib": "2.8.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
